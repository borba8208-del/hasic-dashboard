import pandas as pd
import os

def split_cenik_by_category(path="data/ZBOZI-synchro.csv"):
    # Načtení CSV
    df = pd.read_csv(path, sep=",", encoding="utf-8", low_memory=False)

    # Ověření, že sloupec SKUPINA existuje
    if "SKUPINA" not in df.columns:
        return "Chybí sloupec SKUPINA."

    # Výstupní složka
    out_dir = "data/ceniky"
    os.makedirs(out_dir, exist_ok=True)

    # Pro každou kategorii vytvořit samostatný CSV
    for cat in df["SKUPINA"].dropna().unique():
        df_cat = df[df["SKUPINA"] == cat]

        # Vybereme jen sloupce, které potřebujeme
        df_export = df_cat[["NAZEV", "CENA_P1", "S_JEDNOTKY", "SKUPINA"]].copy()
        df_export.columns = ["nazev", "cena", "jednotka", "kategorie"]

        # Uložit
        safe_cat = cat.replace(" ", "_").replace("/", "_")
        out_path = os.path.join(out_dir, f"cenik_{safe_cat}.csv")
        df_export.to_csv(out_path, sep=";", encoding="utf-8", index=False)

    return f"Rozděleno do {len(df['SKUPINA'].dropna().unique())} souborů."
